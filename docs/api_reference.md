# API Reference

This document provides a high-level overview of the RL-A2A HTTP API endpoints.

Endpoints

- `GET /health` — Healthcheck
- `GET /agents` — List agents
- `POST /agents` — Create agent
- `POST /a2a/messages` — Send messages between agents

For full interactive API docs, enable OpenAPI/Swagger at `/docs` when running the server.
